spring.application.name=yoga-attendance

# MySQL Configuration
# Railway provides MYSQL_URL which includes username, password, host, port, and database
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/yoga_attendance?createDatabaseIfNotExist=true}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Koushik5@}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:YourSecretKeyForJWTTokenGenerationMustBeLongEnough}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kishorekishore2145y@gmail.com}
spring.mail.password=${MAIL_PASSWORD:znyh uiex dmyz bppo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Server Configuration
server.port=${PORT:9000}
server.ssl.enabled=false

# Security Headers
server.error.include-message=never
server.error.include-stacktrace=never

# Security: All credentials must be set as environment variables
# Required: DB_PASSWORD, JWT_SECRET, MAIL_USERNAME, MAIL_PASSWORD
