spring.application.name=yoga-attendance

# MySQL Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/yoga_attendance?createDatabaseIfNotExist=true}
spring.datasource.username=root
spring.datasource.password=Koushik5@
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:YourSecretKeyForJWTTokenGenerationMustBeLongEnough}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kishorekishore2145y@gmail.com}
spring.mail.password=${MAIL_PASSWORD:znyh uiex dmyz bppo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Server Configuration
server.port=9000

# Security: Use environment variables for production
# Set DB_PASSWORD, JWT_SECRET, MAIL_USERNAME, MAIL_PASSWORD as environment variables
