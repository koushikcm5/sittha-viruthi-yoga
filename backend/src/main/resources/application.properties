spring.application.name=yoga-attendance

# MySQL Configuration
# Railway provides MYSQL_URL in format: mysql://user:pass@host:port/db
# Spring needs: jdbc:mysql://host:port/db
spring.datasource.url=jdbc:${MYSQL_URL:mysql://root:Koushik5@@localhost:3306/yoga_attendance}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false


# JWT Configuration
jwt.secret=${JWT_SECRET:7K9mP2nQ5rS8tU1vW4xY6zA3bC5dE7fG9hJ2kL4mN6pQ8rS0tU2vW4xY6zA8bC0dE2fG4hJ6kL8mN0pQ2rS4tU6vW8xY}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Email Configuration - Using Mailtrap for reliable delivery
spring.mail.host=${MAIL_HOST:sandbox.smtp.mailtrap.io}
spring.mail.port=${MAIL_PORT:2525}
spring.mail.username=${MAIL_USERNAME:koushikkaalai123@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Mahendran5@}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.transport.protocol=smtp

# Server Configuration
server.port=${PORT:8080}
server.ssl.enabled=false

# Security Headers
server.error.include-message=never
server.error.include-stacktrace=never
