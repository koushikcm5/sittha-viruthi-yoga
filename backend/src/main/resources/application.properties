spring.application.name=yoga-attendance

# MySQL Configuration (Railway provides MYSQL_URL)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/yoga_attendance?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Koushik5@}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# JWT Configuration
jwt.secret=${JWT_SECRET:7K9mP2nQ5rS8tU1vW4xY6zA3bC5dE7fG9hJ2kL4mN6pQ8rS0tU2vW4xY6zA8bC0dE2fG4hJ6kL8mN0pQ2rS4tU6vW8xY}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kishorekishore2145y@gmail.com}
spring.mail.password=${MAIL_PASSWORD:znyh uiex dmyz bppo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Server Configuration
server.port=${PORT:8080}
server.ssl.enabled=false

# Security Headers
server.error.include-message=never
server.error.include-stacktrace=never
